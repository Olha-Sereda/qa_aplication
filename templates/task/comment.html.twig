{% extends 'base.html.twig' %}

{% block title %}
    {{ 'action_add_comment'|trans({'%id%': task.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'action_add_comment'|trans({'%id%': task.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'POST', action: url('task_comment', {id: task.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('task_show', {id: task.id}) }}" class="btn btn-link">
            {{ 'action_back_to_task'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}