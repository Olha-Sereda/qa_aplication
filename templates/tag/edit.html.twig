{% include 'default/_form.html.twig' with {
    page_title: 'title.category_edit'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'button.action_edit'|trans,
    back_to_list_path: 'category_index',
} %}