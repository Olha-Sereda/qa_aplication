{% if is_granted('ROLE_ADMIN') %}
{% include 'default/_form.html.twig' with {
    page_title: 'user.action_create'|trans,
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'user_index',
} %}
{% endif %}