{% include 'default/_form.html.twig' with {
    page_title: 'title.confirm_best_answer'|trans({'%id%': answer.id|default('')}),
    form: form,
    submit_label: 'button.action_best_answer'|trans,
    back_to_list_path: 'question_index',
} %}